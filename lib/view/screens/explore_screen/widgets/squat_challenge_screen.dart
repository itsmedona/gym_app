import 'package:flutter/material.dart';

class SquatChallengeScreen extends StatelessWidget {
  const SquatChallengeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Text('Squat Challenge'),
    );
  }
}
