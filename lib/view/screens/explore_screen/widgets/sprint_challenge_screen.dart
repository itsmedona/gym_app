import 'package:flutter/material.dart';

class SprintChallengeScreen extends StatelessWidget {
  const SprintChallengeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}